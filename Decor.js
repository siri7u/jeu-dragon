/** VARIABLES **/

/** Gestion du décor **/
let imgFond;
let ecranX = 700;
let ecranY = 600;

function creeDecor(){

    //Taille de la fenetre
    createCanvas(ecranX, ecranY);
    //Fond d'écran
    background(10);

}